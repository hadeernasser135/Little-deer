# نظام حماية كتاب Little Deer — دليل الاستخدام (النسخة المحدّثة)

## إيه الجديد في النسخة دي
- تأكيد الإيميل بقى حقيقي (Firebase بيبعت رسالة فعلاً)
- كل طالب بياخد **كود ثابت** (زي LD-482715) بعد ما توافقي عليه
- المعلم يقدر "يبحث" بالكود، يشوف اسم الطالب، ويبعتله طلب ربط
- الطالب بيوافق أو يرفض الطلب من صفحته
- بعد الموافقة، الطالب يظهر تلقائي في لوحة المعلم
- تقدري تحددي لكل طالب أنهي كتب مسموح له يفتحها (Grammar 1 لـ 4)
- زودت زرار "إيقاف" في لوحتك، منفصل عن "رفض"

## الملفات
- `login.html` → تسجيل الدخول/الحساب (فيه تأكيد إيميل تلقائي دلوقتي)
- `dashboard.html` → صفحة الطالب/المعلم بعد الدخول (كود الطالب، طلبات الربط، الخ)
- `teacher-dashboard.html` → صفحة جديدة: المعلم يضيف طالب بالكود ويشوف طلابه
- `admin.html` → لوحتك: موافقة/تجديد/إيقاف/رفض + تحديد الكتب المسموحة
- `firestore.rules` → قواعد الأمان المحدّثة (لازم تستبدلي بيها القديمة)
- باقي الملفات زي ما هي

---

## الخطوة الوحيدة المطلوبة منك دلوقتي

1. افتحي Firebase Console → Firestore Database → تبويب **Rules**
2. امسحي اللي موجود، وانسخي محتوى `firestore.rules` الجديد وحطيه بدله
3. **Publish**

بعد كده، ارفعي كل الملفات دي تاني على نفس رابط Netlify بتاعك (اسحبيها فوق اللي موجود، هتستبدل القديم تلقائي).

---

## إزاي تجربي السيناريو كامل

1. افتحي `login.html` وسجّلي حساب طالب تجريبي بإيميل تاني (مش إيميلك)
2. هتوصلك رسالة تأكيد على الإيميل ده — افتحيها ودوسي على الرابط
3. ارجعي `dashboard.html` → هيقولك "قيد المراجعة"
4. سجّلي دخول بحسابك (`hadeernasser135@gmail.com`) وافتحي `admin.html`
5. دوسي على اسم الطالب التجريبي → "تفعيل سنة" → هيظهرله كود زي LD-XXXXXX
6. حددي له الكتب المسموحة واحفظي
7. سجّلي حساب معلّم تجريبي تاني (بإيميل ثالث)، فعّليه من admin.html برضه
8. سجّلي دخول بحساب المعلّم → "لوحة الطلاب" → دوري بكود الطالب اللي عملتيه → "إرسال طلب ربط"
9. سجّلي دخول بحساب الطالب → هيشوف الطلب في `dashboard.html` → "قبول"
10. ارجعي لحساب المعلّم → الطالب هيظهر في "طلابي"

## ملحوظة أمانة عن حدود النسخة دي
- التحقق من صحة إجابات الأسئلة لسه مش هيتم على سيرفر (ده محتاج باقة Firebase مدفوعة/Cloud Functions) — النسخة دي بتتحقق في المتصفح، وده مناسب لحجم استخدامك الحالي
- بيانات ربط المعلم بالطالب (مين مرتبط بمين) مقروءة من أي حساب مسجل دخول في النظام، مش بيانات حساسة لكن مش مقفولة 100% — مناسب لعدد محدود من المستخدمين اللي انتي عارفاهم، مش لموقع عام مفتوح
